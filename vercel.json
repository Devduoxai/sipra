{
  "installCommand": "npx pnpm@10.33.4 install && npx prisma generate",
  "buildCommand": "npx prisma generate && next build",
  "crons": [
    {
      "path": "/api/cron/generate",
      "schedule": "0 13 * * *"
    }
  ]
}
